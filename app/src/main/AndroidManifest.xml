<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.genadidharma.github">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.genadidharma.github.READ_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:name=".util.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="true"></receiver>

        <activity android:name=".ui.settings.SettingsActivity" />

        <provider
            android:name=".provider.GithubContentProvider"
            android:authorities="com.genadidharma.github"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.genadidharma.github.READ_DATABASE" />

        <activity
            android:name=".ui.userdetail.UserDetailActivity"
            android:label="@string/title_activity_user_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.usersearch.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>